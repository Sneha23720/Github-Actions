name: Validate
on: 
  pull_request:
    branches:
      - develop
    paths: 
      - 'force-app/**'
jobs:
 validate:
  runs-on: ubuntu-latest
  steps:
   - uses: actions/checkout@v3
   - name: Validate
     uses: Sneha23720/Github-Actions@main
     with:
      SFDX_AUTH_URL: ${{secrets.SFDX_AUTH_URL}}
      DRY_RUN: true
      TEST_LEVEL: NoTestRun
